clip @ git+https://github.com/openai/CLIP.git@dcba3cb2e2827b402d2701e7e1c7d9fed8a20ef1
git+https://github.com/facebookresearch/sam2.git
git+https://github.com/haoheliu/AudioLDM.git

# Audio evaluation metrics (FD, FAD, KL)
# IMPORTANT: audioldm_eval has a broken dependency chain (ssr_eval -> wave -> MySQL-python)
# MySQL-python is Python 2 only and causes ModuleNotFoundError: No module named 'ConfigParser'
#
# Installation workaround:
#   1. pip install ssr_eval --no-deps
#   2. pip install git+https://github.com/haoheliu/audioldm_eval.git --no-deps
#
# This installs ssr_eval without its dependencies, then audioldm_eval finds it already installed
frechet_audio_distance
scikit-image
absl-py
dalle2-pytorch # need this to accomodate the pretrained DALLE2 prior from LAION
librosa
numpy
scikit-video
scipy
torch
torchaudio
torchvision
ultralytics
wandb
wav2clip
kneed
transformers
textblob
torchcodec
